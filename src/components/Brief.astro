---
/**
 * "W skrócie": the three sentences at the top of a content page that tell a
 * reader what is here and what to do. Pass `<li>` children — three at most.
 * This is the site's easy-to-read layer, so the sentences are short and plain.
 */
---

<div class="brief">
  <p>W skrócie</p>
  <ul>
    <slot />
  </ul>
</div>
